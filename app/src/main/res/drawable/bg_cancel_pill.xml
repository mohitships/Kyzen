<?xml version="1.0" encoding="utf-8"?>
<!-- Amber tinted pill for the elapsed time label in the cancel confirmation dialog -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="100dp" />
    <solid android:color="@color/kyzen_amber_bg" />
    <stroke
        android:width="1dp"
        android:color="@color/kyzen_amber_dark" />
</shape>
