<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_child_dashboard.xml — ViewPager2 host
    ────────────────────────────────────────────────
    Two swipeable pages:
      Page 0 — fragment_dashboard.xml  (Hero card + Action grid)
      Page 1 — fragment_activity.xml   (App usage list)

    Dot indicators at the bottom reflect the current page.
    No BottomNavigationView — dots are minimal and child-friendly.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/kyzen_green_bg_light">

    <!-- ViewPager2 — fills all space above the dot indicator -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPagerDashboard"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/dotIndicatorBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Dot indicator bar — three dots, centred at the bottom -->
    <LinearLayout
        android:id="@+id/dotIndicatorBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingTop="12dp"
        android:paddingBottom="20dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Dot 0 — Dashboard (active by default) -->
        <View
            android:id="@+id/dot0"
            android:layout_width="10dp"
            android:layout_height="10dp"
            android:background="@drawable/bg_dot_active"
            android:layout_marginEnd="8dp" />

        <!-- Dot 1 — Activity -->
        <View
            android:id="@+id/dot1"
            android:layout_width="10dp"
            android:layout_height="10dp"
            android:background="@drawable/bg_dot_inactive"
            android:layout_marginEnd="8dp" />

        <!-- Dot 2 — Gem History -->
        <View
            android:id="@+id/dot2"
            android:layout_width="10dp"
            android:layout_height="10dp"
            android:background="@drawable/bg_dot_inactive" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
